/**
 * A debounce time-ot tartalmazó konstans.
 */
export const debounceTimes = {
  none: 0,
  sm: 150,
  md: 600,
  lg: 1000,
};

/**
 * A timeoutot tartalmazó konstans.
 */
export const timeoutMs = {
  sm: 150,
  md: 600,
  lg: 1000,
};

/**
 * Throttletime-ot tartalmazó konstans
 */
export const throttleTimeMs = {
  refresh: 600000,
};
