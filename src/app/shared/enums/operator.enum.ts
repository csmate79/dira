/**
 * operátor típusokat definiáló enum.
 */

export enum OperatorType {
  EQUALS = 'EQUALS',
  GREATER_THAN_EQUALS = 'GREATER_THAN_EQUALS',
  GREATER_THAN = 'GREATER_THAN',
  LESS_THAN_EQUALS = 'LESS_THAN_EQUALS',
  LESS_THAN = 'LESS_THAN',
}
